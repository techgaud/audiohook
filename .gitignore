# environments and caches
.venv/
__pycache__/
*.pyc

# generated work and content channels. NEVER publish: chapters.json holds full book
# text, which is copyrighted for any non-public-domain title.
work/
inbox/
library/

# any book or audio file, anywhere in the tree
*.epub
*.m4b
*.mp3
*.wav
*.aac
*.flac

# Internal knowledge (docs/, CLAUDE.md, CHARTER.md) is versioned centrally in the
# internal gitea repo techgaud/projects-docs, never in the codebase repos. The
# files stay on disk (the CLAUDE cascade still resolves), they are only untracked
# here. Preventive; applies at every depth.
CLAUDE.md
CHARTER.md
docs/
